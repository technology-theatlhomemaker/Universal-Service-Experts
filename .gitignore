.DS_Store
*.tar.gz
*.zip
rebuild/
.vscode/
.idea/

# Local config — generated by scripts/build-config.sh
.env
public_html/assets/config.js

# Server-only credentials — generated by scripts/build-secrets.sh.
private/secrets.php

# Legacy location (pre-move) — kept ignored so a stale local copy can never
# be re-tracked. Safe to remove once any old local file has been deleted.
public_html/api/secrets.php

# Per-machine Claude Code settings
.claude/settings.local.json
