# Defense in depth: this directory must NEVER be served on production.
# Apache 2.4
<RequireAll>
  Require all denied
</RequireAll>
# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
