# Apps Script Web App URL — paste the /exec URL from Deploy → New deployment.
# Use the /dev URL only for local owner-authenticated testing.
APPS_SCRIPT_URL=https://script.google.com/macros/s/REPLACE_ME/exec
